server:
  port: 8888
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/study_notes?useUnicode=true&characterEncoding=UTF-8
    username: root
    password:

    #?????????????
    druid:
      #???ó??????С????С?????
      initial-size: 5
      min-idle: 5
      max-active: 20
      #??????????????????
      max-wait: 60000
      #???ü????ò??????μ????????????????????????
      time-between-eviction-runs-millis: 60000
      #????????????????С????????
      min-evictable-idle-time-millis: 300000
      validation-query: select 'x'
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      #??PSCache,????????????????PSCache???С
      pool-prepared-statements: false #?????Oracle?????poolPreparedStatements?????true??mysql?????????false????????????????????????false??
      max-pool-prepared-statement-per-connection-size: 20
      #???ü??????????filters????????????sql??????
      filters: stat
      #???connectProperties????????mergeSql???????SQL???
      connection-properties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
      #??????DruidDataSource????????
      #spring.datasource.useGlobalDataSourceStat=true

  jpa:
    show-sql: true

logging:
  level:
    com.puzhibing.study_notes.mapper: DEBUG  #配置mybatis下的打印sql语句（扫描mapper包下的mapper接口）